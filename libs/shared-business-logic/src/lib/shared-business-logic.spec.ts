import { sharedBusinessLogic } from './shared-business-logic';

describe('sharedBusinessLogic', () => {
  it('should work', () => {
    expect(sharedBusinessLogic()).toEqual('shared-business-logic');
  });
});
